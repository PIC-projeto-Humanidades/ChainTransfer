# Módulo Identify

Fornece identificação única do nó para diferenciação na rede.

## Controller (identify.controller.ts)

- **GET /device/id**: Retorna o ID deste dispositivo (hardcoded ou configurável).

